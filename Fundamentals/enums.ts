enum seatSelection {        // the aisle will be initially zero(0)
    aisle = "aisle",        // and the next values will be in increased by 1
    middle = 1,             // we can assign value like string to these
    window,
    fourth
}

const mySeat = seatSelection.aisle